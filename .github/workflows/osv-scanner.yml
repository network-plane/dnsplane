name: OSV-Scanner

on:
  pull_request:
    branches: [master]
  schedule:
    - cron: "44 5 * * 3" # weekly Wednesday

permissions:
  actions: read
  security-events: write
  contents: read

jobs:
  osv-scan:
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@c5996e0193a3df57d695c1b8a1dec2a4c62e8730 # v2.3.3
    with:
      scan-args: |-
        ./
